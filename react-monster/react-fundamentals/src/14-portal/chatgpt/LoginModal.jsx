export default function LoginModal() {
	return (
		<div className="modal">
			<h2>Login</h2>

			<input placeholder="Username" />

			<input placeholder="Password" />

			<button>Login</button>
		</div>
	);
}
