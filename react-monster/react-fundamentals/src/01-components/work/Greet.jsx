/**
 * Create a Greet Component
 * Create a new file called Greet.jsx & inside that file, write a Greet component using the function declaration syntax.
 * Afterward, refactor the Greet component to use the arrow function syntax.
 * Finally, register the Greet component in your BasicStateCounterApp.jsx file so that you can see the result when running the app.
 * */


const Greet = () => {
    return (
        <h1>Greet</h1>
    )
}
export default Greet
