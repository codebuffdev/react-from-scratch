/**
 * Component :
 * Components are independent & reusable bits of code.
 * They serve the same purpose as JS functions, but work in isolation & return HTML.
 * */

function ComponentApp() {
    return (
        <section>Hi</section>
    )
}

/*
const App = () => (
    <section>Hello</section>
);
*/

export default ComponentApp;