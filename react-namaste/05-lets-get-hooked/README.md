# React + Vite + Bun 

## React Hooks 
Normal **JS** function. 
Local state variable ⇒ scope inside the component 
normalJs variable, setterMethodOfVariable = useState() ⇒ named import from "react"
When ever state variable changes react rerender the component. 


